# Copy this file to .env.local (which is gitignored) and fill in.
# .env.local is ONLY read when running `vercel dev` for the /api function.
# For plain `npm run dev`, the frontend just falls back to text output.

GEMINI_API_KEY=your_google_ai_studio_key_here
